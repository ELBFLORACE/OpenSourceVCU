/**
 * This file was generated by a vcu_can_generator 39.4.5.
 */

#include <string.h>

#include "generated/Can1.hpp"

#include "vcu_can_msgs_yourcar17/msg/can1_bps_ebs_pressure.hpp"
#include "vcu_can_msgs_yourcar17/msg/can1_apps_bse_steering.hpp"

void Can1Data::receive(struct can_frame *frame, int bytes_read)
{

    if (frame->can_id == 0x12) {
        // Decode can1 frame id 0x12 message BPS_EBSPressure
        if (can1_0x12_bps_ebs_pressure_t::unpack(
            &this->can1_0x12_bps_ebs_pressure,
            frame->data, bytes_read) != 0) {
            // Handle error
        }

        auto msg = this->can1_0x12_bps_ebs_pressure.to_msg();

        msg.stamp = this->node->now();

        can1_0x12_bps_ebs_pressure_pub->publish(msg);
    } else if (frame->can_id == 0x11) {
        // Decode can1 frame id 0x11 message APPS_BSE_Steering
        if (can1_0x11_apps_bse_steering_t::unpack(
            &this->can1_0x11_apps_bse_steering,
            frame->data, bytes_read) != 0) {
            // Handle error
        }

        auto msg = this->can1_0x11_apps_bse_steering.to_msg();

        msg.stamp = this->node->now();

        can1_0x11_apps_bse_steering_pub->publish(msg);
    }
}


void can1DataInitializePublishers(Can1Data* reader, rclcpp::Node::SharedPtr node)
{
   reader->can1_0x12_bps_ebs_pressure_pub = node->create_publisher<vcu_can_msgs_yourcar17::msg::Can1BpsEbsPressure>("/sensor/can1/bps_ebs_pressure", 3);
   reader->can1_0x11_apps_bse_steering_pub = node->create_publisher<vcu_can_msgs_yourcar17::msg::Can1AppsBseSteering>("/sensor/can1/apps_bse_steering", 3);
}

vcu_can_msgs_yourcar17::msg::Can1BpsEbsPressure can1_0x12_bps_ebs_pressure_t::to_msg() {
    auto src_p = this;
    auto msg = vcu_can_msgs_yourcar17::msg::Can1BpsEbsPressure();

    // msg.stamp = this->node->now();

    msg.ebs_pressure1 = (float)(src_p->ebs_pressure1) * 0.025;
    msg.ebs_pressure2 = (float)(src_p->ebs_pressure2) * 0.025;
    msg.brake_pressure_front = (float)(src_p->brake_pressure_front) * 0.05;
    msg.brake_pressure_rear = (float)(src_p->brake_pressure_rear) * 0.05;

    return msg;
}

size_t send_on_can(int fd, const vcu_can_msgs_yourcar17::msg::Can1BpsEbsPressure& msg) {
    struct can_frame msg_frame;
    msg_frame.can_id = 0x12;
    msg_frame.len = 6;

    struct can1_0x12_bps_ebs_pressure_t tmp;
    can1_0x12_bps_ebs_pressure_t::from_msg(&tmp, msg);
    tmp.pack(msg_frame.data, 6);

    return write(fd, &msg_frame, sizeof(struct can_frame));
}

void can1_0x12_bps_ebs_pressure_t::from_msg(
    struct can1_0x12_bps_ebs_pressure_t *dst_p,
    const vcu_can_msgs_yourcar17::msg::Can1BpsEbsPressure &src_p) {

    dst_p->ebs_pressure1 = (uint16_t)((src_p.ebs_pressure1) / 0.025);
    dst_p->ebs_pressure2 = (uint16_t)((src_p.ebs_pressure2) / 0.025);
    dst_p->brake_pressure_front = (uint16_t)((src_p.brake_pressure_front) / 0.05);
    dst_p->brake_pressure_rear = (uint16_t)((src_p.brake_pressure_rear) / 0.05);

}

vcu_can_msgs_yourcar17::msg::Can1AppsBseSteering can1_0x11_apps_bse_steering_t::to_msg() {
    auto src_p = this;
    auto msg = vcu_can_msgs_yourcar17::msg::Can1AppsBseSteering();

    // msg.stamp = this->node->now();

    msg.brakeforce = (float)(src_p->brakeforce) * 0.5;
    msg.steering_angle = (float)(src_p->steering_angle) * 0.09;
    msg.apps_left = (float)(src_p->apps_left) * 0.05;
    msg.apps_right = (float)(src_p->apps_right) * 0.05;

    return msg;
}

size_t send_on_can(int fd, const vcu_can_msgs_yourcar17::msg::Can1AppsBseSteering& msg) {
    struct can_frame msg_frame;
    msg_frame.can_id = 0x11;
    msg_frame.len = 7;

    struct can1_0x11_apps_bse_steering_t tmp;
    can1_0x11_apps_bse_steering_t::from_msg(&tmp, msg);
    tmp.pack(msg_frame.data, 7);

    return write(fd, &msg_frame, sizeof(struct can_frame));
}

void can1_0x11_apps_bse_steering_t::from_msg(
    struct can1_0x11_apps_bse_steering_t *dst_p,
    const vcu_can_msgs_yourcar17::msg::Can1AppsBseSteering &src_p) {

    dst_p->brakeforce = (uint16_t)((src_p.brakeforce) / 0.5);
    dst_p->steering_angle = (uint16_t)((src_p.steering_angle) / 0.09);
    dst_p->apps_left = (uint16_t)((src_p.apps_left) / 0.05);
    dst_p->apps_right = (uint16_t)((src_p.apps_right) / 0.05);

}
